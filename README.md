run exmaple.ts which should reproduce the problem

You should see a progress bar succeeding for the first stream
And a second progress bar start, then the program exits with 0, without it finishing fully
